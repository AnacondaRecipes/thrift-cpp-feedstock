anaconda_config_version: 1
upstream_sources:
  - webpage:
      url: https://dlcdn.apache.org/thrift/{}/thrift-{}.tar.gz
      regex: (?:.*)thrift-(.*)\.tar\.gz
    packages:
      - libthrift
      - thrift-compiler
      - thrift-cpp
