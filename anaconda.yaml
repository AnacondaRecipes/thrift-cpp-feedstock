anaconda_config_version: 1
upstream_sources:
  - webpage:
      url: https://thrift.apache.org/download
      regex: thrift-([\d.]+)\.tar\.gz
    packages:
      - libthrift
      - thrift-compiler
      - thrift-cpp
